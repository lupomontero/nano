const {
  firebaseReducer,
  firebaseMiddleWare,
  nanoFirebase,
} = require('../src/firebase');


describe('firebaseReducer', () => {
  it('should...', () => {
    expect(typeof firebaseReducer).toBe('function');
  });
});


describe('firebaseMiddleWare', () => {
  it('should...', () => {
    expect(typeof firebaseMiddleWare).toBe('function');
  });
});


describe('nanoFirebase', () => {
  it('should', () => {
    expect(typeof nanoFirebase).toBe('function');
  });
});
